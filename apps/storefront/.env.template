# Copy this file to `.env`. Replace the host port after `bun run services:start`
# if Compose assigns a random port to this worktree.
DATABASE_URL=postgresql://postgres:password@localhost:5432/mze-store

# These values configure the Account service that runs in the Storefront.
BETTER_AUTH_SECRET=local-development-auth-secret-32-characters
BETTER_AUTH_URL=http://localhost:3001
CORS_ORIGIN=http://localhost:3001
