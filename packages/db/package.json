{
  "name": "@mze-store/db",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "development": "./src/index.ts",
      "node": "./dist/src/index.js",
      "default": "./src/index.ts"
    },
    "./*": {
      "types": "./dist/src/*.d.ts",
      "development": "./src/*.ts",
      "node": "./dist/src/*.js",
      "default": "./src/*.ts"
    }
  },
  "scripts": {
    "build": "vp run -w package-db-build",
    "db:push": "vp run -w package-env-build && drizzle-kit push",
    "db:generate": "vp run -w package-env-build && drizzle-kit generate",
    "db:studio": "vp run -w package-env-build && drizzle-kit studio",
    "db:migrate": "vp run -w package-env-build && drizzle-kit migrate",
    "check-types": "vp run -w package-db-check-types"
  },
  "dependencies": {
    "@mze-store/env": "workspace:*",
    "dotenv": "catalog:",
    "drizzle-orm": "^0.45.2",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@mze-store/config": "workspace:*",
    "@types/pg": "^8.20.0",
    "drizzle-kit": "^0.31.10",
    "typescript": "catalog:"
  }
}
