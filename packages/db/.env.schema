# drizzle-kit reads this contract. It needs the connection string, which the root
# schema composes from the discovered PostgreSQL parts.
#
# @import(../../)
# @generateTsTypes(path=./env.ts, exposeEnv=local)
# @defaultSensitive=false
# @defaultRequired=infer
# ---
